extends layout

block content 
    h1 게시판
    a(href='/new') 새 게시글 작성
    ul
        each post in posts 
            li
                a(href=`/edit/${post.id}`)  #{post.title}
                form(action=`/delete/${post.id}?_method=DELETE`, method='post')
                    button(type='submit') 삭제